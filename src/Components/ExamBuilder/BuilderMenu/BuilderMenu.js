import classes from "./BuilderMenu.module.css";

const BuilderMenu = () => {
  return (
    <div className={classes.exambuilder__menu}>
      <span>Exam Builder Menu</span>
    </div>
  );
};

export default BuilderMenu;
